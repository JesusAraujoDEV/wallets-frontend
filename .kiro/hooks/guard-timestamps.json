{
  "version": "v1",
  "hooks": [
    {
      "name": "Estimation timestamp guard",
      "trigger": "PreToolUse",
      "description": "Validates newly recorded estimation timestamps against the machine clock when metrics are enabled.",
      "matcher": "fs_write|str_replace|fs_append",
      "action": {
        "type": "command",
        "command": "node hooks/kiro-guard-timestamps.js",
        "timeout": 10
      }
    }
  ]
}